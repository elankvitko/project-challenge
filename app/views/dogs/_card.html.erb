<div class="col-md-3 d-flex align-items-stretch">
  <div class="card mb-4 shadow-sm">
    <% if dog %>
      <%= render partial: 'thumbnail', locals: { dog: dog } %>
    <% else %>
      <%= render partial: 'thumbnail', locals: { dog: false } %>
    <% end %>
  </div>
</div>
